pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx}"
      run: pnpm exec eslint {staged_files}
    format:
      glob: "*.{ts,tsx,css,md}"
      run: pnpm exec prettier --write {staged_files} && git add {staged_files}

